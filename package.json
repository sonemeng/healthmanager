{
  "name": "openvitals",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "db:generate": "turbo db:generate --filter=@openvitals/database",
    "db:migrate": "turbo db:migrate --filter=@openvitals/database",
    "db:seed": "turbo db:seed --filter=@openvitals/database",
    "desktop:dev": "pnpm --filter @healthmanager/desktop dev",
    "desktop:build": "pnpm --filter @healthmanager/desktop build",
    "desktop:dist": "pnpm --filter @healthmanager/desktop dist",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,css,md}\""
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "turbo": "^2.8.17",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  }
}
